import React from "react";

import Timeline from "../timeline/Timeline";

const ResumeComponent = ({ date, field, adition, name }) => {
  return (
    <div>
      <Timeline />
    </div>
  );
};

export default ResumeComponent;
